<resources>

    <style name="MaterialLoginTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <style name="MaterialLoginTheme.SelectableItemTheme">
        <item name="colorControlHighlight">@color/register</item>
    </style>
    <style name="MaterialLoginTheme.SelectableItemBackground">
        <item name="android:theme">@style/MaterialLoginTheme.SelectableItemTheme</item>
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>

    <style name="MaterialLoginTheme.TextAppearanceRegister">
        <item name="android:textColor">@color/login</item>
        <item name="colorControlNormal">@color/login</item>
        <item name="android:textColorHint">@color/login</item>
        <item name="colorControlActivated">@color/login</item>
        <item name="colorControlHighlight">@color/login</item>
    </style>
</resources>
